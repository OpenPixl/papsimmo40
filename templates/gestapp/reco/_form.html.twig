{{ form_start(form) }}
    <div class="row">
        <div class="col-4">
            <h3>Prescripteur</h3>
        </div>
        <div class="col-4">
            <h3>Vendeur</h3>
        </div>
        <div class="col-4">
            <h3>Définition du bien</h3>
        </div>
    </div>
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
