<form method="post" action="{{ path('app_gestapp_transaction_add_coll_transac_delete', {'id': add_coll_transac.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ add_coll_transac.id) }}">
    <button class="btn">Delete</button>
</form>
