{% extends 'base.html.twig' %}

{% block title %}AddCollTransac{% endblock %}

{% block body %}
    <h1>AddCollTransac</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ add_coll_transac.id }}</td>
            </tr>
            <tr>
                <th>PourcentComm</th>
                <td>{{ add_coll_transac.pourcentComm }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestapp_transaction_add_coll_transac_index') }}">back to list</a>

    <a href="{{ path('app_gestapp_transaction_add_coll_transac_edit', {'id': add_coll_transac.id}) }}">edit</a>

    {{ include('gestapp/transaction/add_coll_transac/_delete_form.html.twig') }}
{% endblock %}
