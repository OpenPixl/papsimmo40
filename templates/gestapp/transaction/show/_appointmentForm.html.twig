{{ form_start(form) }}
<div class="row g-3 align-items-center">
    <div class="col-4">
        {{ form_label(form.appointment) }}
    </div>
    <div class="col-3">
        {{ form_widget(form.appointment, {'attr': {'class':'flatpickr flatpickr-input'}}) }}
    </div>
</div>
{{ form_end(form) }}