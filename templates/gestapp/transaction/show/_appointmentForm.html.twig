{{ form_start(form) }}
<div class="row g-3 align-items-center">
    <div class="col-8">
        {{ form_label(form.appointment) }}
    </div>
    <div class="col-4">
        {{ form_widget(form.appointment, {'attr': {'class':'form-control form-control-sm'}}) }}
    </div>
</div>
{{ form_end(form) }}