{{ form_start(form) }}

    <div class="row mb-1 mt-1 g-1">
        {{ form_label(form.docAnnulation, '', {'label_attr': {'class':'col-sm-12 form-label fw-semibold mb-0 border-bottom border-dark'}}) }}
        <div class="col-12">
            {{ form_widget(form.docAnnulation, {'attr': {'class': 'form-control form-control-sm'}}) }}
        </div>
    </div>

    {{ form_rest(form) }}
{{ form_end(form) }}