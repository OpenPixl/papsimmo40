<div class="col-12">
    <ul>
        {% for customer in customers %}
            <li>{{ customer.lastName }} {{ customer.firstName }} | {{ customer.zipcode }} | <a id="addCustomerSearch" class="addcustomersearch btn btn-sm btn-light openModal" href="{{path('op_gestapp_customer_addsearchcustomer', {'id': customer.id, 'idproperty': idproperty})}}"><i class="fa-light fa-user-check"></i></a></li>
        {% else %}
        {% endfor %}
    </ul>
</div>