<section class="property-edit">
    <div class="row g-2 mt-1">
        <div class="col-sm-3">
            <div class="mb-1">
                <a id="btnAddCustomer" href="{{ path('op_gestapp_customer_addcustomerjson', {'idproperty': property.id}) }}" class="btn btn-sm btn-outline-warning w-100 openModal" data-bs-data="ADDCUSTOMER-Ajouter une nouvelle fiche client">
                    Ajouter un vendeur<br>
                    sur ce bien.
                </a>
            </div>
            <div class="card text-dark border-dark">
                {{ render(controller('App\\Controller\\Admin\\SearchController::customerProperty', {'idproperty': property.id })) }}
            </div>

        </div>
        <div class="col-sm-9">
            <div id="listeCustomers" class="row g-1">
                {% include 'gestapp/customer/include/_listecustomers.html.twig' with {'idproperty': property.id } %}
            </div>
            <div class="row" id="listeSearchCustomers">

            </div>
        </div>
    </div>
</section>