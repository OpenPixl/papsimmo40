{% extends 'base.html.twig' %}

{% block title %}OtherOption{% endblock %}

{% block body %}
    <h1>OtherOption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ other_option.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ other_option.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestapp_choice_other_option_index') }}">back to list</a>

    <a href="{{ path('app_gestapp_choice_other_option_edit', {'id': other_option.id}) }}">edit</a>

    {{ include('gestapp/choice/other_option/_delete_form.html.twig') }}
{% endblock %}
