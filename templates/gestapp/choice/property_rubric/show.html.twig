{% extends 'base.html.twig' %}

{% block title %}propertyRubric{% endblock %}

{% block body %}
    <h1>propertyRubric</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ property_rubric.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ property_rubric.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ property_rubric.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestapp_choice_property_rubric_index') }}">back to list</a>

    <a href="{{ path('app_gestapp_choice_property_rubric_edit', {'id': property_rubric.id}) }}">edit</a>

    {{ include('gestapp/choice/property_rubric/_delete_form.html.twig') }}
{% endblock %}
