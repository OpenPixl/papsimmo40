{% extends 'base.html.twig' %}

{% block title %}propertyRubricss{% endblock %}

{% block body %}
    <h1>propertyRubricss</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ property_rubricss.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ property_rubricss.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ property_rubricss.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestapp_choice_property_rubricss_index') }}">back to list</a>

    <a href="{{ path('app_gestapp_choice_property_rubricss_edit', {'id': property_rubricss.id}) }}">edit</a>

    {{ include('gestapp/choice/property_rubricss/_delete_form.html.twig') }}
{% endblock %}
