{% extends 'base.html.twig' %}

{% block title %}PropertyEquipement{% endblock %}

{% block body %}
    <h1>PropertyEquipement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ property_equipement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ property_equipement.name }}</td>
            </tr>
            <tr>
                <th>Cat</th>
                <td>{{ property_equipement.Cat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestapp_choice_property_equipement_index') }}">back to list</a>

    <a href="{{ path('app_gestapp_choice_property_equipement_edit', {'id': property_equipement.id}) }}">edit</a>

    {{ include('gestapp/choice/property_equipement/_delete_form.html.twig') }}
{% endblock %}
