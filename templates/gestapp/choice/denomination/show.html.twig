{% extends 'base.html.twig' %}

{% block title %}Denomination{% endblock %}

{% block body %}
    <h1>Denomination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ denomination.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ denomination.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestapp_choice_denomination_index') }}">back to list</a>

    <a href="{{ path('app_gestapp_choice_denomination_edit', {'id': denomination.id}) }}">edit</a>

    {{ include('gestapp/choice/denomination/_delete_form.html.twig') }}
{% endblock %}
