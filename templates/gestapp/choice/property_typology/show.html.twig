{% extends 'base.html.twig' %}

{% block title %}PropertyTypology{% endblock %}

{% block body %}
    <h1>PropertyTypology</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ property_typology.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ property_typology.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestapp_choice_property_typology_index') }}">back to list</a>

    <a href="{{ path('app_gestapp_choice_property_typology_edit', {'id': property_typology.id}) }}">edit</a>

    {{ include('gestapp/choice/property_typology/_delete_form.html.twig') }}
{% endblock %}
