{% extends 'base.html.twig' %}

{% block title %}PropertyState{% endblock %}

{% block body %}
    <h1>PropertyState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ property_state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ property_state.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('op_gestapp_choice_property_state_index') }}">back to list</a>

    <a href="{{ path('op_gestapp_choice_property_state_edit', {'id': property_state.id}) }}">edit</a>

    {{ include('gestapp/choice/property_state/_delete_form.html.twig') }}
{% endblock %}
