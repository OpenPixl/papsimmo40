<form method="post" action="{{ path('op_gestapp_choice_property_state_delete', {'id': property_state.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ property_state.id) }}">
    <button class="btn">Delete</button>
</form>
