{% extends 'base.html.twig' %}

{% block title %}CatDocument{% endblock %}

{% block body %}
    <h1>CatDocument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_document.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cat_document.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestapp_choice_cat_document_index') }}">back to list</a>

    <a href="{{ path('app_gestapp_choice_cat_document_edit', {'id': cat_document.id}) }}">edit</a>

    {{ include('gestapp/choice/cat_document/_delete_form.html.twig') }}
{% endblock %}
