{% extends 'admin.html.twig' %}

{% block title %}Hello protexaController!{% endblock %}

{% block contentTitle %}
    <div class="row d-flex justify-content-between" >
        <div class="col-sm-12">
            <h1>Registre de transaction</h1>
        </div>
    </div>
{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-12">
            <a id="btnAddReco" class="btn btn-outline-primary btn-sm" href="#" data-bs-whatever="ADD-Ajout d'une recommandation">Ajouter une recommandation</a>
            {% if is_granted('ROLE_PRESCRIBER') %}
                <a id="btnModalPrescriber" class="btn btn-sm btn-outline-dark" href="#">Modifier mes informations personnel et ma carte d'identité</a>
            {% endif %}
            {% if is_granted('ROLE_SUPER_ADMIN') %}
                <a class="btn btn-sm btn-outline-dark" href="#">Liste des prescripteurs</a>
            {% endif %}
        </div>
    </div>

    <div class="row">
        <div class="col-12" id="liste">
            {{ include('soap/protexa/include/_liste.html.twig') }}
        </div>
    </div>
{% endblock %}
