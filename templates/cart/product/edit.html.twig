{% extends 'admin.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-12">
            <h1>Support Commerciaux - <small>Edition d'un produit</small></h1>
        </div>
    </div>

    <div class="navigation d-flex justify-content-between">
        <div class="mb-1">
            <a id="btnreturn" href="{{ path('op_cart_product_index') }}" class="btn btn-sm btn-outline-dark" >retour</a>
        </div>
    </div>

    {{ include('cart/product/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('cart/product/_delete_form.html.twig') }}
{% endblock %}
