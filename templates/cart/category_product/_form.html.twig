{{ form_start(form) }}
<div class="row mb-1 mt-1 g-1">
    <div class="col-4">
        {{ form_label(form.name, '', {'label_attr': {'class':'form-label'}}) }}
    </div>
    <div class="col-6">
        {{ form_widget(form.name, {'attr': {'class': 'form-control form-control-sm'}}) }}
    </div>
</div>
{{ form_end(form) }}
